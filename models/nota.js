class nota {

    constructor(titulo, texto, userId, id){
        this.titulo = toString(titulo);
        this.texto = toString(texto);
        this.userId = toString(userId);
        this.id = toString(id)
    }

    crearNota(){
    }

    modificarNota(){

    }

    eliminarNota(){
        
    }
}