class user{
    constructor(nombre, apellido, mail, password, id){
        this.nombre = toString(nombre);
        this.apellido = toString(apellido);
        this.mail = toString(mail);
        this.password = toString(password);
        this.id = toString(id); 
    }

    crearUser(){
        
    }

    verificarUser(){

    }
    
    modificarUser(){

    }

    eliminarUser(){

    }
}